/*
 * DAQ System for Rocket-GPS-INS-Fusion
 * Assigned to: Omar
 */

void setup() {
  Serial.begin(115200);
  // Initialize sensors (IMU, GPS, etc.)
}

void loop() {
  // Read sensor data
  // Send data over Serial in a structured format (e.g., CSV)
  // Serial.print(accelX); Serial.print(","); ...
  delay(10);
}
